/**
 * Design-system primitives. Import from "@/components/ui".
 * These encode the Apple-style system — prefer them over ad-hoc markup so the
 * app stays visually coherent. See DESIGN_SYSTEM.md.
 */
export { Button, buttonVariants, type ButtonProps } from "./button";
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
  type CardProps,
} from "./card";
export { Badge, badgeVariants, type BadgeProps } from "./badge";
export { StatusBadge, FlagBadge, type StatusBadgeProps } from "./status-badge";
export { Input, type InputProps } from "./input";
